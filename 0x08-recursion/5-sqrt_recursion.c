#include "main.h"
/**
 * _sqrt_recursion - will return x square root of n.
 * @n: a variable with type int
 * Return: int
 */
int _sqrt_recursion(int n)
{
	int root = 0;
	int root = n;
	if (root != dup)
}
