#include <stdlib.h>
#include "main.h"
/**
 * strtow - it is a main function
 * @str:  array with type char
 * Return: char
 */
char **strtow(char *str)
{
	char **words;
	
	if (str == NULL || str == "")
		return (NULL);

}
